import AppShell from "@/components/shell/AppShell";

// D5: intentionally a stub — no market data or heatmap in this pass
const News = () => (
  <AppShell>
    <div className="container mx-auto px-4 py-24 text-center">
      <h1 className="text-4xl font-bold text-foreground mb-3">Research &amp; News</h1>
      <p className="text-muted-foreground text-lg">Coming soon.</p>
    </div>
  </AppShell>
);

export default News;
